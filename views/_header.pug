header.header
    nav.nav.nav--tours
        a.nav__el(href='/') All Tours
    .header__logo 
        img(src='/img/logo-white.png' alt='Natours logo') 
    nav.nav.nav--user
        if user 
            button.nav__el.nav__el--logout(id='logoutBtn') Log Out  
            a.nav__el(href='/me')
                if user.image
                    img.nav__user-img(src=`/img/users/${user.image}` alt=`User Poto of ${user.name}`)
                else
                    img.nav__user-img(src=`/img/users/default.jpg` alt=`User Poto of ${user.name}`)    
                span= user.name.split(' ')[0]
        else         
            a.nav__el(href='/login') Log In 
            a.nav__el.nav__el--cta Sign Up